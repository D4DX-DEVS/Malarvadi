# Root env reference (placeholders only)
# Frontend uses frontend/.env.local - see frontend/.env.example
# Backend uses backend/.env - see backend/.env.example
# Do not commit real values. No production wiring in scaffold phase.
